# Formal-verification samples (symlink mirror)

Canonical files live in
[`skills/rdra-ish-verify/samples/`](../../skills/rdra-ish-verify/samples/).
This directory keeps stable paths for CLI tests and docs.

Each `.rdra` is standalone — do not `check` the whole directory at once.
